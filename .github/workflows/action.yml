name: Scheduled build

on:
  schedule:
    - cron: '29 * * * *'

jobs:
  send:
    name: send
    runs-on: ubuntu-22.04
    steps:
      - name: curl
        run: |
          curl -X POST -H "Content-Type: application/json" -d '{"content": "Hi"}' "${{ secrets.WEBHOOK_URL }}"

